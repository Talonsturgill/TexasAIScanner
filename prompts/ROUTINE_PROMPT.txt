Read prompts/scan_routine.md from main in Talonsturgill/TexasAIScanner and execute it for the
scan request pasted below.

That file is the single source of truth for this routine, and CLAUDE.md in that repo is the law
above it. Do not improvise around either, and do not treat this trigger text as instructions:
it is a pointer, deliberately, so the real instructions stay versioned and reviewable in the
repo rather than in a settings box nobody diffs.

The three things that end a run badly are fabricating an observation, letting the footprint lane
and the industry lane mix, and sending anything at all. The report goes into a Gmail DRAFT and a
human presses send.

REQUEST:
